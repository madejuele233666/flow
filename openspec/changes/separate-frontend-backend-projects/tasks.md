## 1. [CORE] 工作区骨架与迁移检查点

- [x] 1.1 **【检查点】** 向用户确认最终迁移边界：根目录保留 `openspec/`、`docs/` 等共享资产，仅把应用源码、应用测试和项目元数据迁入 `frontend/` 与 `backend/`。在用户明确同意前，不执行物理移动。
- [x] 1.2 创建 `frontend/` 和 `backend/` 目录骨架，并为两边准备各自的项目元数据文件、README 和基础目录。
- [x] 1.3 盘点当前根目录中的应用级文件与共享级文件，形成迁移清单，标明每个文件/目录的目标工作区或保留位置。

## 2. [CORE] 后端工作区迁移

- [x] 2.1 将后端源码与运行时资产迁入 `backend/`，至少包括 `flow_engine/`、后端项目元数据和后端专属测试。
- [x] 2.2 修正后端工作区内的脚本入口、测试路径、文档命令和相对路径引用，确保 `flow` CLI、Daemon 和 TUI 仍从 `backend/` 工作区运行。
- [x] 2.3 在 `backend/` 工作区执行最小验证：可编辑安装或等价导入检查、`flow` 命令可见性检查、后端相关测试或 smoke check。

【防腐规定】后端迁移阶段严禁为了兼容旧路径而同时保留根级和 `backend/` 两套有效后端源码；`flow_engine` 只能保留一个真实位置。

## 3. [CORE] 前端工作区迁移

- [x] 3.1 将 HUD 前端源码与前端专属资产迁入 `frontend/`，至少包括 `flow_hud/`、HUD 配置样例和 HUD 相关测试。
- [x] 3.2 修正 `frontend/` 工作区内的启动命令、测试路径、配置路径和文档，使 HUD 包能够独立安装和运行。
- [x] 3.3 在 `frontend/` 工作区执行最小验证：前端包导入检查、HUD 相关测试或 smoke check，并确认前端无需依赖 `backend/` 源码导入。

【防腐规定】前端迁移后严禁新增 `import flow_engine` 或 `from flow_engine ...` 形式的跨工作区源码依赖；前后端边界继续以 IPC 为准。

## 4. [EDGE] 根目录导览与最终清理

- [x] 4.1 将根级 README 改为仓库导览文档，明确哪些命令需要在 `frontend/` 执行，哪些命令需要在 `backend/` 执行。
- [x] 4.2 删除或替换不再适合作为根级运行入口的旧元数据、旧测试路径和重复目录，确保仓库根不再表现为单一 Python 应用。
- [x] 4.3 **【检查点】** 向用户展示迁移后的根目录结构、前后端各自的入口命令和验证结果，获得确认后再进入后续实现或收尾修改。

## 5. [EDGE] 验证与回归记录

- [x] 5.1 分别记录 `frontend/` 与 `backend/` 的安装、运行、测试命令，补充到相应工作区 README 或开发说明中。
- [x] 5.2 运行一次最终验证矩阵，至少覆盖根目录结构检查、后端入口检查、前端入口检查和关键测试路径检查。
- [x] 5.3 若存在无法在当前环境完成的验证，明确记录阻塞原因和后续补验步骤。
