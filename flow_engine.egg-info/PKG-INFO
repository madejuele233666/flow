Metadata-Version: 2.4
Name: flow-engine
Version: 0.1.0
Summary: 心流引擎 — 用严苛的单任务协议对抗多任务焦虑
License: MIT
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: click>=8.1
Requires-Dist: toml>=0.10
Requires-Dist: gitpython>=3.1
Requires-Dist: httpx>=0.27
Provides-Extra: ai
Requires-Dist: openai>=1.0; extra == "ai"
Provides-Extra: tui
Requires-Dist: textual>=0.50; extra == "tui"
Provides-Extra: dev
Requires-Dist: pytest>=8.0; extra == "dev"
Requires-Dist: pytest-cov>=4.0; extra == "dev"
Requires-Dist: ruff>=0.3; extra == "dev"

# 心流引擎 (Flow Engine)

**心流引擎** 是一个极简、自律且充满黑客（Geek）风格的终端任务管理与生产力系统。它基于 **严格的单一任务独占（Single-Tasking）** 协议设计，致力于用严苛的逻辑流转消除因多任务切换带来的认知损耗与现代职业焦虑。

## ✨ 核心特性矩阵

### 1. 严格防打扰的八维状态机
系统内置了严格的任务八维状态模型：`Draft` → `Ready` → `Scheduled` → `In Progress` → `Paused` → `Blocked` → `Done` → `Canceled`。
* **唯一排他原则**：系统在物理级别限制同时只能有一个任务处于 `In Progress` 状态。
* **自动压栈机制**：当你强制开启新任务时，系统会自动将当前活跃任务转入 `Paused`（暂停）状态。

### 2. Markdown + Git 纯文本时光机
完全抛弃了笨重的关系型数据库。所有任务树、优先级信息均以标准的 `tasks.md` 持久化存储。
配合无头的后台守护进程（Ghost Daemon），系统的任何修改都会在后台静默执行 `git commit`。实现时光机级别的防灾回滚以及跨设备的私有云同步。

### 3. 六边形架构与纯异步化设计
经历过深度的架构重构，这套系统具有极佳的工程美学：
* **六边形架构**：外部接口（CLI/TUI）完全通过 `FlowClient` 端口协议与核心领域引擎交互，实现彻底解耦。
* **纯异步 I/O**：全面支持 `asyncio` 和 `asyncclick`，大幅提升读写及 IPC（进程间通信）性能。
* **守护进程模式**：拥有 `LocalClient`（本地单机模式）和 `RemoteClient`（Daemon RPC 模式）两种自动透明切换的运行体态。

### 4. 强大的插件接口与钩子系统
系统架构了受 Pluggy 启发的**强类型钩子框架**，且包含了熔断保护策略。
你可以通过社区开放的标准接口为系统编写插件，例如针对不同软件（如 VS Code、浏览器）的“现场捕捉插件”，在任务切换时自动捕获上下文路径，并在任务恢复时一键复现当时的工作现场。

### 5. AI 引力分发与智能降维 (规划/部分支持)
* **动态引力算法 (Gravity Score)**：调度器根据优先级（P0-P3）和截止日期（DDL）自动计算出接下来最建议处理的任务。
* **AI 大模型解构**：允许携带自有大语言模型 API Key (BYOK)。对于复杂的任务，使用 `flow breakdown` 命令，系统瞬间为你将其降维拆解为 15 分钟级别的小步骤。

---

## 🛠️ CLI 指令集参考

你将通过 `flow` 终端指令作为入口与心流引擎交互：

### 核心流转指令
```bash
flow add "任务名称" --ddl "2026-10-01" --p 1   # 添加新任务
flow ls                   # 打印按“引力得分”排序的高优任务列表
flow start <TaskID>       # 核心魔法：打断当前任务 -> 自动压栈并触发快照 -> 启动新任务 -> 自动 Git 提交
flow status               # 查看当前的心流专注状态及已专注时长
flow block <TaskID> --reason "等待审批"   # 将任务标记为因外部依赖卡住
flow pause                # 主动暂停当前任务
flow resume <TaskID>      # 恢复暂停/阻塞中的任务
flow done                 # 完成当前活跃任务并归档
```

### 智能与高阶命令
```bash
flow breakdown <TaskID>   # 让 AI 帮你把任务拆解成步骤
flow templates ls         # 查看可用任务模板
flow plugins ls           # 查看系统中已安装的外挂插件
flow export --format json # 导出当前的任务库数据
```

### 守护进程管理
```bash
flow daemon start         # 在后台启动 Ghost Daemon 常驻进程 (IPC 服务)
flow daemon stop          # 停止后台运行的进程
flow daemon status        # 查看守护进程是否在线
```

---

## 🚀 未来战略 (Roadmap)

我们正致力于在 `V2.0` 重塑更多可能：
1. **MCP (Model Context Protocol) 彻底打通**：将心流引擎升级为 MCP 服务器。允许你的全局 AI（如 Claude Desktop）直接与当前的专注环境互动，交接任务物理上下文。
2. **本地 RAG 专属记忆胶囊**：当你在一项任务上专注时，静默将你的查阅痕迹转化为任务专属向量数据库，为日后跨期工作恢复提供 AI 即时回溯。
3. **沉浸式的 UI 面板 (TUI/HUD)**：引入抬头显示器风格的非侵入式极简悬停 UI 和全面的 Terminal TUI 面板。

---

> *"用严苛的协议包裹思维，以无感的机制守护专注。"*
