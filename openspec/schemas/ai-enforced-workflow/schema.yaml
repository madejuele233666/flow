name: ai-enforced-workflow
version: 7
description: Spec-driven workflow with artifact review, source-first implementation review, optional repository-index cache support, reusable working reviewer sessions, and challenger-pass closure
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: >
      Create the proposal document that establishes WHY this change is needed.


      Sections:

      - **Why**: 1-2 sentences on the problem or opportunity.
      - **What Changes**: Bullet list of concrete capability, workflow, skill,
        or contract changes.
      - **Capabilities**: Identify which specs will be created or modified.
      - **Risk Tier**: Classify the change as `LIGHT`, `STANDARD`, or `STRICT`
        using concrete triggers.
      - **Impact**: Affected code, APIs, dependencies, systems, and skills.


      IMPORTANT:

      - The Capabilities section is the contract between proposal and specs.
      - The Risk Tier controls how strict later design and verification steps
        must be.
      - Do not use abstract labels like "improve architecture" without naming
        concrete capabilities or artifacts that will change.
    requires: []

  - id: specs
    generates: specs/**/*.md
    description: Detailed specifications for the change
    template: spec.md
    instruction: >
      Create specification files that define WHAT the system should do.


      Create one spec file per capability listed in the proposal's Capabilities
      section.

      - New capabilities: use the exact kebab-case name from the proposal.
      - Modified capabilities: use the existing spec folder name from
        `openspec/specs/<capability>/`.


      Delta operations (use `##` headers):

      - **ADDED Requirements**
      - **MODIFIED Requirements**
      - **REMOVED Requirements**
      - **RENAMED Requirements**


      Format requirements:

      - Each requirement: `### Requirement: <name>`
      - Use SHALL/MUST for normative requirements
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - Every requirement MUST have at least one scenario


      For workflow and architecture-related capabilities, requirements MUST be
      testable as document or process checks, not only as code behavior.
    requires:
      - proposal

  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: >
      Create the design document that explains HOW to implement the change.


      When to include design.md (create if any apply):

      - Cross-cutting change or new architectural pattern
      - New external dependency or significant data model change
      - Security, performance, migration, or workflow-gating complexity
      - Ambiguity that benefits from technical decisions before coding
      - Proposal risk tier is `STANDARD` or `STRICT`


      Required sections:

      - **Context**
      - **Goals / Non-Goals**
      - **Decisions**
      - **Risks / Trade-offs**
      - **Migration Plan**
      - **Open Questions**


      Each key Decision MUST include:

      - the problem being solved
      - alternatives considered
      - why this option was chosen
      - at least one verification hook


      ANTI-ABSTRACTION GUARDRAILS:

      For each important mechanism or boundary, provide:

      - **Stack Equivalent**
      - **Named Deliverables**
      - **Failure Semantics**
      - **Boundary Examples**
      - **Contrast Structure**
      - **Verification Hook**


      SKILL ORCHESTRATION:

      - If the change references, aligns with, or replicates an existing
        system, use `openspec-align` before finalizing design decisions.
      - Use `openspec-architect` for architectural comparisons and stack
        equivalents.
      - All verification stages MUST use the shared sequence
        `verify-sequence/default` from
        `openspec/schemas/ai-enforced-workflow/verification-sequence.md`.
      - Repository-index support uses `index-sequence/default` only as optional
        cache discovery or refresh, never as a Stage 0 gate.
      - `openspec-artifact-verify` and `openspec-verify-change` MUST stay thin
        and delegate orchestration to the shared sequence.
      - Human-only confirmation does not satisfy a verification gate.


      INDEPENDENT VERIFIER PLAN (required for `STANDARD` and `STRICT`):

      For each independent verification step, design.md MUST document:

      - sequence reference:
        `openspec/schemas/ai-enforced-workflow/verification-sequence.md#verify-sequence/default`
      - cache-helper reference:
        `openspec/schemas/ai-enforced-workflow/index-sequence.md#index-sequence/default`
      - verifier agent path:
        `.codex/agents/verify-reviewer.toml`
      - index-maintainer agent path:
        `.codex/agents/index-maintainer.toml`
      - index skill entry points:
        `openspec-index-preflight` and `openspec-index-maintain`
      - verifier invocation method:
        built-in subagent API in the main process
      - agent spawn decision contract:
        `agent-spawn-decision-v1`
      - verifier invocation template:
        `verify-reviewer-inline-v2`
      - minimal verification bundle fields:
        `change / mode / review_phase / review_pass_type / risk_tier /
        evidence_paths_or_diff_scope / findings_contract`
      - optional cache handoff fields:
        `index_context.contract / index_context.manifest_path /
        index_context.manifest_present / index_context.preflight_report_path /
        index_context.cache_mode / index_context.fallback_policy`
      - output-path fields:
        `output_paths.findings_path / output_paths.verifier_evidence_path /
        output_paths.gemini_raw_path? / output_paths.gemini_report_path? /
        output_paths.spawn_decision_path?`
      - execution evidence fields:
        `review_phase / review_pass_type / cache_mode / closure_authority /
        verifier_output_path / reviewed_paths / skipped_paths / reviewed_axes /
        unreviewed_axes / coverage_status / saturation_status /
        early_stop_reason? / skip_reasons?`
      - implementation-only inline evidence:
        `review_scope / review_coverage`
      - session semantics:
        artifact review may block implementation entry;
        implementation reruns stay in the working session;
        challenger is the only implementation closure authority
      - Gemini policy:
        optional only when repository or checkpoint policy explicitly enables
        dual review; not a default Stage A gate
      - runner contract details when Gemini is used:
        `gemini-capture`, raw report path, normalized report path, recovery
        semantics, and command-resolution evidence


      REVIEW GATE:

      Before finalizing design.md, include enough structure for artifact review,
      implementation review, inline implementation scope/coverage, and
      challenger-pass closure through `verify-sequence/default`. Missing
      guardrails that affect a `STRICT` change are blocking.
    requires:
      - proposal

  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: >
      Create the task list that breaks down the implementation work.


      **IMPORTANT: Follow the checkbox format exactly.**

      - Group related tasks under numbered `##` headings
      - Each task MUST be a checkbox: `- [ ] X.Y Task description`
      - Order tasks by dependency
      - Keep tasks anchored to concrete deliverables


      REQUIRED TASK PROPERTIES:

      - Tasks MUST name concrete outputs
      - Tasks MUST include verification work, not only implementation work
      - Tasks for workflow behavior MUST mention how the behavior will be
        exercised or audited
      - Verification tasks in `STANDARD` or `STRICT` changes MUST reference
        `verify-sequence/default`, name the minimal verification bundle fields,
        name optional cache-helper fields, name verifier output/report paths,
        define routing target and caller-local guardrails, and require
        execution evidence with:
        `review_phase / review_pass_type / cache_mode / closure_authority /
        verifier_output_path / reviewed_paths / skipped_paths / reviewed_axes /
        unreviewed_axes / coverage_status / saturation_status /
        early_stop_reason? / skip_reasons?`
      - Implementation verification tasks MUST also require inline
        `review_scope` and `review_coverage`
      - Tasks MUST state that same-session implementation reruns are for
        convergence and challenger is the only implementation closure step
      - Tasks MUST NOT introduce a standalone planner file, planner schema, or
        planner agent as a prerequisite for implementation review


      RISK-TIER GATING:

      - `LIGHT`: verifier-subagent review
      - `STANDARD`: explicit artifact review plus implementation working and
        challenger review
      - `STRICT`: same as `STANDARD`, plus explicit exercises and, only when
        repository policy opts in, Gemini dual-review checkpoints


      CHECKPOINTS:

      Any task group that modifies core workflow gates, verification logic, or
      repair routing MUST end with a checkpoint task in this exact format:

      - [ ] X.Y [Checkpoint] Run verifier-subagent review using
        `verify-sequence/default` for [specific output]. Use repository-index
        cache helper `index-sequence/default` only when cache discovery or
        refresh is useful; do not require cache authority before review.
        Name the minimal verification bundle fields (`change / mode /
        review_phase / review_pass_type / risk_tier /
        evidence_paths_or_diff_scope / findings_contract`), optional
        `index_context` fields (`contract / manifest_path / manifest_present /
        preflight_report_path / cache_mode / fallback_policy`), required
        `output_paths` fields (`findings_path / verifier_evidence_path /
        gemini_raw_path? / gemini_report_path? / spawn_decision_path?`), the
        routing target, and any caller-local guardrails. Require authoritative
        verifier findings JSON plus verifier execution evidence JSON. Require
        implementation checkpoints to include inline `review_scope` and
        `review_coverage`. State that same-session implementation reruns are
        convergence-only and challenger is the only implementation closure
        authority. If the checkpoint explicitly enables Gemini dual review,
        require logical runner contract `gemini-capture`, raw plus normalized
        output, and recovery via `input_raw_path -> report_path`.


      SKILL AWARENESS:

      Reference the expected skills when tasks depend on them
      (`openspec-align`, `openspec-architect`, `openspec-artifact-verify`,
      `openspec-repair-change`, `openspec-verify-change`).
    requires:
      - specs
      - design

apply:
  requires:
    - tasks
  tracks: tasks.md
  instruction: |
    Read context files, work through pending tasks, and mark complete as you go.

    For any verification or checkpoint task in `ai-enforced-workflow`:
    - execute shared sequence `verify-sequence/default`
    - use repository-index helper `index-sequence/default` only for optional
      cache discovery or refresh
    - do not require cache authority before implementation review can start or
      complete
    - use verifier agent `.codex/agents/verify-reviewer.toml` in read-only mode
      with bundle fields:
      `change / mode / review_phase / review_pass_type / risk_tier /
      evidence_paths_or_diff_scope / findings_contract`
    - when cache helper runs, pass `index_context`
      (`contract / manifest_path / manifest_present /
      preflight_report_path / cache_mode / fallback_policy`)
    - pass `output_paths`
      (`findings_path / verifier_evidence_path / gemini_raw_path? /
      gemini_report_path? / spawn_decision_path?`)
    - before any new verifier or index-maintainer spawn, write
      `agent-spawn-decision-v1`
    - require authoritative verifier findings JSON plus verifier execution
      evidence JSON
    - require implementation execution evidence to include inline
      `review_scope` and `review_coverage`
    - require implementation closure to come only from a challenger pass
    - keep same-session implementation reruns in the active working session
      until challenger entry conditions are met
    - allow automatic implementation repair only when
      `mode=implementation && blocking=true &&
      redirect_layer=implementation && auto_fixable=true`
    - route blocking non-implementation findings to `openspec-repair-change`
    - use Gemini only when repository or checkpoint policy explicitly enables
      dual review
    - do not substitute user confirmation for verification
