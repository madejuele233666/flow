## ADDED Requirements

### Requirement: Architecture Documentation
The project MUST include a comprehensive architecture document that describes all 8 layers of the Flow Engine.

#### Scenario: Contributor reads the architecture map
- **WHEN** a contributor or AI opens `docs/architecture.md`
- **THEN** they can read a detailed breakdown of the FlowClient Port, the State Machine Mutex, Typed Event Bus, Hook Breakers, Git Markdown Storage, and IPC daemon system.
